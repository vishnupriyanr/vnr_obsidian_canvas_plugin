# vnr_obsidian_canvas_plugin

# Connection Navigator for Obsidian Canvas

A plugin that helps you navigate and manage connections between nodes on Obsidian Canvas.

## Features

- **View Node Connections**: Select a node and see all its incoming and outgoing connections in a popup panel
- **Navigate Between Nodes**: Click on any connection to jump to that node (go to new node and click your hotkeys again to open panel to go back a connection or go forward a connection)
- **(Future Feature) Toggle Arrow Visibility**: Hide/show all connection arrows on the canvas with a floating button (👁) to reduce visual clutter

## Installation

1. Download the latest `connection-navigator.zip` from this repository
2. Extract the zip file - you'll get a folder called `connection-navigator`
3. Open your Obsidian vault folder and navigate to `.obsidian/plugins/`
   - If the `plugins` folder doesn't exist, create it
4. Move the `connection-navigator` folder into the `plugins` folder
5. Open Obsidian and go to **Settings → Community plugins**
6. Make sure "Restricted mode" is turned OFF
7. Find "Connection Navigator" in the list and toggle it ON

## Usage

### View Connections
- **Ribbon icon**: Click the git-branch icon (⎇) in the left sidebar while a node is selected
- **Command palette**: Press `Cmd/Ctrl + P` and search for "Show connections for selected canvas node"
- **Hotkey**: Set a custom hotkey in Settings → Hotkeys (search for "connection")

## Future Features:
### Toggle Arrow Visibility
- Click the **eye icon** (👁) that appears on the right side of the canvas
- Click again to show arrows

## Author

Vishnupriya Napa Ravikumar (Vishnu)
